const service = (cardData) => {

  return new Promise ((resolve, reject) => {

    /*reject({
      message: "Erro ao gerar token do cartão.",
      data: err
    })*/
    resolve("12345678909-0909")
  })
}

export default service